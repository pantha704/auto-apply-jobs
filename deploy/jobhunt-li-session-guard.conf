[Service]
# worker_linkedin exits 12 after requeueing the claimed job when the
# saved LinkedIn session is revoked or challenge-gated. Do not hammer the
# login/authwall by restarting repeatedly; require explicit session renewal.
RestartPreventExitStatus=12
